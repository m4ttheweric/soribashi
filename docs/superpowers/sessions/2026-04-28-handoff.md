# Wave 1 — Session Handoff (2026-04-28)

> **For the next agent session:** read this doc + the playbook to recover full context. The Wave 1 PR is open and substantive — don't redo work; pick from "What's next" below.

## Where things stand

**Branch:** `wave-1-token-consolidation-button-pilot` (currently checked out in the primary checkout at `/Users/matt/Documents/GitHub/soribashi/`).
**PR:** [#1](https://github.com/m4ttheweric/soribashi/pull/1) — open, not yet merged.
**Commits:** 47 on top of `origin/main`. All quality gates clean (809 vitest, 15 Playwright on the pilot project, typecheck silent).

**The branch is rebased onto current `main`** as of `29a9bea` (the user's "clean main" commit). No conflicts pending.

## What this PR delivers

### Pilot app + recipe
- `apps/core-radix-pilot/` — new Vite app on port 5174, standalone from playground.
- Consolidated soribashi theme expressing every signal-classified CVI token (`apps/core-radix-pilot/src/theme/index.ts`).
- Codegen wiring (`apps/core-radix-pilot/soribashi.config.ts`).
- Demo pages: TokenReview, ScreenReplica, ButtonMatrix.
- Button recipe via `definePolymorphicComponent` with full variant × intent × size × state matrix, refs forwarded, focus-visible on all variants, Mantine-parity press-down + loading transition.

### Soribashi codegen improvements (added during PR walkthrough)
- **Dual-emit**: codegen emits `--color-X-Y` (canonical wrapped) + `--__hsl-color-X-Y` (bare-component companion for Tailwind alpha). `EmitCssOptions.emitCompanionHsl: 'auto' | boolean` (default `'auto'` skips companion in v4-only setups).
- Tailwind v3 emit references the `-hsl` companion in the `<alpha-value>` pattern.

### Documentation
- `docs/superpowers/specs/2026-04-26-token-consolidation-and-button-pilot-design.md` — Wave 1 spec.
- `docs/superpowers/plans/2026-04-26-plan-token-consolidation-and-button-pilot.md` — implementation plan.
- `docs/superpowers/pilots/2026-04-26-token-consolidation.md` — Phase 0 journal (894 lines): inventory, classification, decisions, deprecation list, 11 open design questions, visual review.
- `docs/superpowers/pilots/2026-04-26-button-conversion.md` — Phase 1 journal: legacy Button surface, consolidated shape, 7 soribashi gaps (3 RESOLVED post-Wave-1), IconButton/ButtonDropdown extension sketches.
- `docs/superpowers/specs/2026-04-26-core-radix-conversion-playbook.md` — **the engagement deliverable**. 5 sections: methodology, authoring patterns, gaps + C → A bridge, migration stub, future waves outline.
- `docs/superpowers/reviews/2026-04-27-pr-1-review.md` — codex code review (caught the ref-forwarding bug; both findings now resolved).

## Resolved gaps (closed during the walkthrough)

- **Gap 1 — codegen `hsl()` wrapper.** Resolved via dual-emit. Tailwind alpha utilities now work.
- **Gap 4 — focus indicator on transparent variants.** Resolved via variant-scoped `outline-color` override.
- **Gap 7 — ref forwarding.** Caught by codex, fixed via passing `ref` from render ctx to `<Element>`.

Plus refinements: `surface.overlay` → `surface.scrim` (frees `overlay`/`floating` naming for Wave 2), 1px press-down on `:active`, full Mantine loading transition (slide-out inner + blur veil + spinner slide-in), spinner sized per data-size.

## Open gaps (still need work, but not blocking Wave 1)

- ~~**Gap 2**~~ — **DOCUMENTED AS CONVENTION (2026-05-01).** Mantine's recipes destructure the seven framework keys alongside own props; auto-stripping rejected because composition needs forward-ability. Pattern lives in playbook § 2.1 "Render body destructure." See conversion journal § 4 Gap 2.
- ~~**Gap 3**~~ — **DOCUMENTED (2026-05-01).** Canonical pilot-app test scaffolding template (vitest config + jest-dom setup file + workspace entry + dev deps) lives in playbook § 2.0 "Pilot app test scaffolding." Future Wave 2-4 plans copy from there. See conversion journal § 4 Gap 3.
- **Gap 4 (semantic-token)** — `accent.feedback` has no clean home in soribashi `SemanticTokens` shape. Nice-to-have.
- **Gap 5** — `borderColor.DEFAULT` codegen reset pattern undocumented. Nice-to-have.
- **Gap 7 (type ergonomics half)** — `PolymorphicComponentProps` ref type is a tangled intersection; `createRef<T>()` doesn't assign without casts. Open.

## Open design questions (need design owner input)

11 questions in token-consolidation journal § 5. The five that meaningfully gate decisions:
- **Q1** — collapse `card` + `popover` into `surface.default`, or keep two?
- **Q2** — primary 50-400 ramp regenerated; was the original hue drift intentional?
- **Q3** — `text.{default, muted, subtle, disabled}` vs CVI's `text.{primary, secondary, tertiary, disabled}`?
- **Q5** — Wave 1 dark scope `.dark` vs CVI's `.dark .claim-view-islands`. Deferred to integration project (needs codegen feature).
- **Q11** — surface ↔ foreground pairing: formalize per-surface or rely on usage-site discipline?

Async work — ping design owner, get input, then small follow-up commits if anything overrides the Wave 1 default.

## Worktree state

User removed the dedicated worktree mid-session. The wave-1 branch is currently checked out in the **primary checkout** at `/Users/matt/Documents/GitHub/soribashi/`. No `.worktrees/` directory exists.

If you need isolation again:
```bash
git checkout main
git worktree add .worktrees/wave-1-token-consolidation-button-pilot wave-1-token-consolidation-button-pilot
cd .worktrees/wave-1-token-consolidation-button-pilot && bun install
```

## What's next — pick one of these (ordered by leverage)

### A. Wave 2 — Tooltip pilot (transient overlay compound)
The next category in the playbook. Needs a fresh brainstorm + spec + plan because soribashi doesn't yet have a compound-authoring primitive. Wave 2's first task is the design question: "how do we author a Radix-anatomy compound with `defineComponent`?"

Now that Gap 1 is resolved, Wave 2 can use Tailwind alpha utilities cleanly (modal scrim, tooltip ghost backgrounds, focus-ring tints) — that wasn't possible during Wave 1's planning.

**Start with:** `/superpowers:brainstorming` against "Wave 2 Tooltip pilot — what does it look like?" Reference playbook § 5 for sizing.

### B. Address remaining open soribashi gaps (Gap 5, 7-types)
Smaller bounded fixes:
- ~~Gap 2~~ — Documented as convention 2026-05-01; no code change. See playbook § 2.1.
- ~~Gap 3~~ — Documented as transferable scaffold template 2026-05-01 in playbook § 2.0; no code change.
- **Gap 5** — make architectural choice: codegen emits `*` border-color reset, or document explicit-application requirement
- **Gap 7 (types)** — audit `PolymorphicComponentProps` ref type, replace tangled intersection with `Ref<T>`

Could be done in any order, individually.

### C. Codegen feature additions for Option A (the C → A bridge)
The two additive features the playbook calls out:
1. **Configurable dark-mode selector** in the codegen config (so host's `.dark .claim-view-islands` scope can be expressed instead of hardcoded `.dark`)
2. **Config-level pass-through** for `corePlugins.preflight`, `plugins`, `content` globs

After both ship, the pilot's `tailwind.config.js` collapses to one line: `module.exports = require('./src/generated/tailwind.config.generated.js')`. Option A north star reached.

### D. Get design owner input on open questions Q1, Q2, Q3, Q11
Async work — review the 5 questions in journal § 5, get human design decisions, apply small follow-up commits if the Wave 1 default is overridden.

### E. Real CVI integration project — migrate the 115 importers
The biggest follow-up. Its own brainstorm + spec + plan + impl cycle. Sized in playbook § 4 across 5 phases (A-E, S/M/S/M/L sizing). Involves touching assured-dev for the first time.

### F. Merge Wave 1
PR is mergeable as-is. All gates pass. Self-contained. Approve + merge to main, then start Wave 2 against a clean main.

## Quick context recovery for the next session

```bash
# 1. Get on the branch (it's already checked out in primary)
cd /Users/matt/Documents/GitHub/soribashi
git status
git log --oneline -5

# 2. Read the playbook (the synthesis)
cat docs/superpowers/specs/2026-04-26-core-radix-conversion-playbook.md

# 3. If the next session is Wave 2: read the future-wave outline
sed -n '/Wave 2/,/Wave 3/p' docs/superpowers/specs/2026-04-26-core-radix-conversion-playbook.md

# 4. Verify gates still clean
bun run typecheck && bun run test && bun run test:browser
```

Or just start the next session with: *"Read `docs/superpowers/sessions/2026-04-28-handoff.md` and let's continue from there."*
