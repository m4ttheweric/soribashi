node_modules/
.bun/
.worktrees/

dist/
build/
*.tsbuildinfo

coverage/
.nyc_output/

test-results/
playwright-report/

.claude/settings.local.json
.claude/worktrees/
.local-dev/

.vscode/
.idea/
*.swp
*.swo

.DS_Store
Thumbs.db

.env
.env.local

*.log
npm-debug.log*
bun-debug.log*
test/dist

# vitest browser-mode failure screenshots. These land in a dir named after the
# FAILING TEST FILE, so tier-2 captures go to <Name>.test.tsx/ while the real
# visual baselines go to <Name>.visual.test.tsx/ and must stay tracked. The
# patterns match FILES rather than dirs so the negation can re-include (git
# cannot re-include anything under an excluded directory).
packages/ui/src/**/__screenshots__/*.test.tsx/*.png
!packages/ui/src/**/__screenshots__/*.visual.test.tsx/*.png

# Visual baselines are CI-generated Linux artifacts; local-platform baselines are advisory
# noise. Vitest nests each baseline one level deeper than this pattern originally assumed
# (`__screenshots__/<test-file>/<name>-<browser>-<platform>.png`, confirmed against the
# installed v4.1.10's actual output), hence the `**/` before the filename glob.
packages/ui/src/**/__screenshots__/**/*-darwin.png
packages/ui/src/**/__screenshots__/**/*-win32.png

# Vitest's own scratch dir for toMatchScreenshot mismatch/missing-reference debug images
# (never the committed baselines themselves, see above).
packages/ui/.vitest-attachments/

# superpowers subagent-driven-development scratch workspace
.superpowers/

